{
  "name": "parcel-sandbox",
  "version": "1.0.0",
  "description": "Simple Parcel Sandbox",
  "main": "index.html",
  "scripts": {
    "start": "parcel serve index.html --port 1111",
    "build": "parcel build index.html --no-minify --out-dir ./docs/",
    "local": "npx http-server ."
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.6.1"
  }
}
